<Window x:Class="SmartFillMonitor.Views.Main.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SmartFillMonitor"
        xmlns:viewModels="clr-namespace:SmartFillMonitor.ViewModels.Main"
        xmlns:views="clr-namespace:SmartFillMonitor.Views.Main"
        xmlns:UserControls="clr-namespace:SmartFillMonitor.Views.UserControls"
        mc:Ignorable="d"
        Title="MainView"
        Height="860"
        Width="1360"
        MinHeight="780"
        MinWidth="1220"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        ResizeMode="CanResize"
        Background="{StaticResource AppBgDark}">
    <Window.Resources>
        <DataTemplate DataType="{x:Type viewModels:DashBoardViewModel}">
            <views:DashBoardView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewModels:SimulationViewModel}">
            <views:SimulationView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewModels:DashQueryViewModel}">
            <views:DashQueryView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewModels:LogsViewModel}">
            <views:LogsView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewModels:AlarmsViewModel}">
            <views:AlarmsView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewModels:SettingsViewModel}">
            <views:SettingsView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewModels:AccountSecurityViewModel}">
            <views:AccountSecurityView />
        </DataTemplate>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="18"/>
            <RowDefinition Height="100"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Row="0" Grid.ColumnSpan="2"
                Background="Transparent"
                Panel.ZIndex="10"
                MouseLeftButtonDown="OnChromeMouseLeftButtonDown"/>
          
        <UserControls:NavigationMenu Grid.Column="0" Grid.RowSpan="3"/>
        
        <Border Grid.Column="1" Grid.Row="1" Background="{StaticResource AppBgHeader}" BorderBrush="{StaticResource AppBoderBrush}" BorderThickness="0,0,0,1">
            <Grid Margin="20,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Foreground="{StaticResource AppTextMuted}" VerticalAlignment="Center" FontSize="14">
                    <Run Text="当前生产批次："/>
                    <Run Text="{Binding Header.CurrentBatchNo, Mode=OneWay}" FontWeight="SemiBold"/>
                </TextBlock>
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Right">
                    
                    <UserControls:StatusIndicator StatusText="PLC在线" IsOnline="{Binding Header.IsPlcConnected, Mode=OneWay}" Margin="0,0,20,0"/>
                    
                    <UserControls:StatusIndicator StatusText="数据库写入中" IsWarning="True" IsOnline="True" Margin="0,0,20,0"/>
                    
                    <TextBlock Text="{Binding Header.CurrentTime, Mode=OneWay}" Foreground="{StaticResource AppTextMuted}" FontFamily="Consolas" FontSize="14" VerticalAlignment="Center" Margin="0,0,20,0"/>

                    <UserControls:StatusLight State="{Binding Header.IndicatorState, Mode=OneWay}" Margin="0,0,20,0" VerticalAlignment="Center"/>
                    
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,8,0">
                        <TextBlock Text="👤" FontSize="16" Margin="0,0,5,0"/>
                        <TextBlock Text="{Binding CurrentUserName}" FontSize="16" Margin="0,0,5,0" Foreground="{StaticResource AppTextMuted}" MaxWidth="160" TextTrimming="CharacterEllipsis"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Button Content="修改密码"
                                Style="{StaticResource OutLineButton}"
                                Command="{Binding OpenAccountSecurityCommand}"
                                Margin="0,0,6,0"
                                Padding="8,2"
                                Visibility="{Binding IsUserLoggedIn, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                        <Button Content="{Binding SwitchUserButtonText}"
                                Style="{StaticResource OutLineButton}"
                                Command="{Binding SwitchUserCommand}"
                                Margin="0,0,6,0"
                                Padding="8,2"/>
                        <Button Content="{Binding ExitSystemButtonText}"
                                Style="{StaticResource OutLineButton}"
                                Command="{Binding ExitSystemCommand}"
                                Padding="8,2"/>
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Border>

        <ContentControl Grid.Column="1" Grid.Row="2" Background="{StaticResource AppBgDark}" Margin="20" Content="{Binding MainContent}"/>

    </Grid>
</Window>
